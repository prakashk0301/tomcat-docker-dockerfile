apiVersion: v1
kind: Service
metadata:
    name: prakashktomcatdockerdockerfile
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: prakashktomcatdockerdockerfile